

## 一、 实验内容

本实验旨在通过编程实现 Dijkstra（迪杰斯特拉）算法，解决加权有向图中的单源最短路径问题。实验包含两个具体的测试场景：

1. **场景一（22个基站顶点）**：

   * 使用包含 22 个基站顶点的图数据（对应 Excel 表 0）。
   * 以基站 **567443** 为源点，计算其到图中其它各点的单源最短路径。
   * 计算从源点 **567443** 到目标点 **33109** 的最短路径及长度。
2. **场景二（42个基站顶点）**：

   * 使用包含 42 个基站顶点的图数据（对应 Excel 表 1）。
   * 以基站 **565845** 为源点，计算其到图中其它各点的单源最短路径。
   * 计算从源点 **565845** 到目标点 **565667** 的最短路径及长度。

实验要求能够读取 Excel 格式的邻接矩阵数据，并实现Dijkstra 算法。

## 二、 算法设计与代码实现

### 2.1 算法设计思路

Dijkstra 算法采用贪心策略（Greedy Strategy）。算法维护两个集合：

* $S$：已确定最短路径的节点集合。
* $V-S$：尚未确定最短路径的节点集合。

算法步骤：

1. **初始化**：将源点 $v$ 的距离设为 0，其余点设为无穷大。
2. **贪心选择**：在 $V-S$ 集合中，选取距离源点最近的节点 $u$，将其加入集合 $S$。
3. **松弛更新**：以 $u$ 为中介点，检查是否能缩短从源点到 $u$ 的邻接点 $v$ 的距离。如果 $dist[u] + weight(u, v) < dist[v]$，则更新 $dist[v]$ 并记录前驱节点。
4. **循环**：重复步骤 2 和 3，直到所有节点都被加入 $S$ 或剩余节点不可达。

### 2.2 核心代码实现 (Python)

以下是实验中使用的核心算法代码（基于 `shortest_paths_naive.py`）：

```python
import argparse
from typing import Dict, List, Tuple

import xlrd


def get_sheet(wb, sheet_name: str | None, sheet_index: int | None):
    if sheet_index is not None:
        return wb.sheet_by_index(sheet_index)
    if sheet_name is None:
        return wb.sheet_by_index(0)
    try:
        return wb.sheet_by_name(sheet_name)
    except xlrd.biffh.XLRDError:
        # 允许将数字字符串视为索引
        if sheet_name.isdigit():
            return wb.sheet_by_index(int(sheet_name))
        raise


def load_graph(path: str, sheet_name: str | None, sheet_index: int | None) -> Tuple[List[int], Dict[int, Dict[int, float]]]:
    """
    解析格式类似于提供的邻接矩阵的工作表。
    第一行包含数字索引头，第二行（索引 1）从第 2 列开始包含节点 ID，
    第 2..n+1 行包含权重，-1 表示没有边。
    """
    wb = xlrd.open_workbook(path)
    sh = get_sheet(wb, sheet_name, sheet_index)

    node_ids: List[int] = []
    # 节点 ID 位于第 1 行（索引 1），从第 2 列（索引 2）开始
    for col in range(2, sh.ncols):
        cell = sh.cell_value(1, col)
        if cell == "" or cell is None:
            continue
        node_ids.append(int(cell))

    graph: Dict[int, Dict[int, float]] = {nid: {} for nid in node_ids}
    for row_idx, nid in enumerate(node_ids, start=2):
        row_node = int(sh.cell_value(row_idx, 1))
        if row_node not in graph:
            continue
        for col_offset, dest in enumerate(node_ids):
            weight = sh.cell_value(row_idx, col_offset + 2)
            if weight == "" or weight is None:
                continue
            if isinstance(weight, (int, float)):
                w = float(weight)
                if w >= 0:
                    graph[row_node][dest] = w
    return node_ids, graph


def dijkstra_naive(graph: Dict[int, Dict[int, float]], source: int, node_ids: List[int]):
    """
    使用朴素的 O(V^2) 方法实现的 Dijkstra 算法，使用数组/列表扫描，
    """
    # 初始化
    # dist[i] 存储从源点 source 到 i 的最短距离
    dist: Dict[int, float] = {node: float("inf") for node in node_ids}
    # prev[i] 存储 i 的前驱节点
    prev: Dict[int, int | None] = {node: None for node in node_ids}
    # s[i] 如果到 i 的最短路径已确定（已访问），则为 True
    s: Dict[int, bool] = {node: False for node in node_ids}

    if source not in graph:
        raise ValueError(f"源点 {source} 不在图中")

    dist[source] = 0.0
  
    # 循环查找所有点的最短路径
    for _ in range(len(node_ids)):
        # A. 贪心选择 (Selection)
        # 在未被访问的点 (V-S) 中，寻找距离源点最近的点 u
        u = None
        min_dist = float("inf")
      
        for node in node_ids:
            if not s[node] and dist[node] < min_dist:
                min_dist = dist[node]
                u = node
      
        # 如果找不到更近的点（说明剩下的点都不可达），提前结束
        if u is None:
            break
          
        s[u] = True
      
        # B. 松弛更新 (Relaxation)
        # 更新 u 的邻居的距离
        # 逻辑上等同于检查 c[u][j] < M。
        for v, w in graph[u].items():
            if not s[v]:
                new_dist = dist[u] + w
                if new_dist < dist[v]:
                    dist[v] = new_dist
                    prev[v] = u

    return dist, prev


def reconstruct_path(prev: Dict[int, int | None], target: int) -> List[int]:
    if target not in prev:
        return []
    path: List[int] = []
    cur: int | None = target
    while cur is not None:
        path.append(cur)
        cur = prev[cur]
    path.reverse()
    return path


def run_once(path: str, sheet: str | None, sheet_index: int | None, source: int, target: int | None):
    node_ids, graph = load_graph(path, sheet_name=sheet, sheet_index=sheet_index)
  
    # 使用朴素实现
    dist, prev = dijkstra_naive(graph, source, node_ids)

    print(f"工作表: {sheet} | 节点数: {len(node_ids)} | 源点: {source}")
    print("源点到各点的距离及路径:")
    for nid in node_ids:
        d = dist.get(nid, float("inf"))
        if d == float("inf"):
            print(f"  {source} -> {nid}: 不可达")
        else:
            path_nodes = reconstruct_path(prev, nid)
            path_str = " -> ".join(map(str, path_nodes))
            print(f"  {source} -> {nid}: 距离 {d:.3f}, 路径 {path_str}")

    if target is not None:
        if dist.get(target, float("inf")) == float("inf"):
            print(f"目标 {target} 从 {source} 不可达")
        else:
            shortest_path = reconstruct_path(prev, target)
            print(f"单独查询目标 {target}: 路径 {' -> '.join(map(str, shortest_path))} (长度 {dist[target]:.3f})")


def main():
    parser = argparse.ArgumentParser(description="LTE 基站图上的单源最短路径 (朴素 O(V^2)).")
    parser.add_argument("--file", default="附件1-1.基站图的邻接矩阵-v1-23.xls", help="Excel 邻接矩阵文件")
    parser.add_argument("--sheet", help="要读取的工作表名称 (例如: 数据1 或 数据2)")
    parser.add_argument("--sheet-index", type=int, help="可选的工作表索引 (从 0 开始)，如果名称不方便使用")
    parser.add_argument("--source", type=int, required=True, help="源顶点 ID")
    parser.add_argument("--target", type=int, help="可选的目标顶点 ID，用于打印路径")
    args = parser.parse_args()

    run_once(args.file, args.sheet, args.sheet_index, args.source, args.target)


if __name__ == "__main__":
    main()

```

## 三、 复杂度分析

假设图中有 $V$ 个顶点（Nodes）和 $E$ 条边（Edges）。

### 3.1 时间复杂度

本实验采用的是**朴素实现（基于数组/列表扫描）**。算法主要包含两个部分：**贪心选择（寻找最小距离点）**和**松弛更新（更新邻居距离）**。

1. **贪心选择**：无论图的结构如何，外层循环执行 $V$ 次，内层寻找最小值的操作每次都需要遍历当前所有未访问节点。这部分的开销始终是 $\sum_{i=1}^{V} i \approx O(V^2)$。
2. **松弛更新**：这部分的开销取决于边的数量 $E$。在整个算法过程中，每条边会被访问一次。

基于此，我们进行分类讨论：

* **最好情况**：

  * **场景**：图极其稀疏，例如 $E \approx 0$（没有边或边很少）。
  * **分析**：松弛操作的开销极小，可以忽略。但由于贪心选择部分必须扫描所有节点，时间复杂度依然由 $O(V^2)$ 主导。
  * **复杂度**：$O(V^2)$。
* **最坏情况**：

  * **场景**：图是稠密图，甚至是完全图，即 $E \approx V^2$。
  * **分析**：此时松弛操作需要处理所有的边，开销为 $O(E) \approx O(V^2)$。加上贪心选择的 $O(V^2)$，总开销依然是 $O(V^2)$ 量级。
  * **复杂度**：$O(V^2)$。
  * *注：在稠密图中，朴素实现的 $O(V^2)$ 往往优于堆优化实现的 $O((V+E)\log V) \approx O(V^2 \log V)$。*
* **平均情况**：

  * **场景**：图的边数介于稀疏和稠密之间。
  * **分析**：总时间复杂度为 $O(V^2 + E)$。由于 $E$ 的上限是 $V^2$，因此平均时间复杂度仍为 $O(V^2)$。

**总结**：对于朴素版 Dijkstra 算法，时间复杂度对图的稀疏度不敏感，稳定在 **$O(V^2)$**。

### 3.2 空间复杂度

空间复杂度主要取决于图的存储方式和辅助数组。

1. **辅助数组**：`dist`, `prev`, `s` 等数组/字典都需要存储 $V$ 个元素，空间为 $O(V)$。
2. **图的存储（邻接表/字典）**：
   * **最好情况**（稀疏图）：当 $E \approx 0$ 时，仅需存储顶点信息，空间复杂度为 $O(V)$。
   * **最坏情况**（稠密图）：当 $E \approx V^2$ 时，需要存储所有边，空间复杂度为 $O(V^2)$。
   * **平均情况**：空间复杂度为 **$O(V + E)$**。

## 四、 实验结果说明

### 4.1 场景一：22个基站顶点 (Sheet 0)

* **运行命令**：
  ```powershell
  python shortest_paths_naive.py --sheet-index 0 --source 567443 --target 33109
  ```
* **运行结果**：

```text
工作表: None | 节点数: 22 | 源点: 567443
源点到各点的距离及路径:
  567443 -> 33109: 距离 1956.926, 路径 567443 -> 566750 -> 567439 -> 33109
  567443 -> 565696: 距离 1343.406, 路径 567443 -> 566783 -> 566993 -> 565696
  567443 -> 566631: 距离 761.938, 路径 567443 -> 566783 -> 566631
  567443 -> 566720: 距离 2111.289, 路径 567443 -> 566750 -> 567439 -> 566751 -> 566720   
  567443 -> 566742: 距离 302.540, 路径 567443 -> 566742
  567443 -> 566747: 距离 1988.138, 路径 567443 -> 566742 -> 566802 -> 567322 -> 566747   
  567443 -> 566750: 距离 683.088, 路径 567443 -> 566750
  567443 -> 566751: 距离 1622.912, 路径 567443 -> 566750 -> 567439 -> 566751
  567443 -> 566783: 距离 344.546, 路径 567443 -> 566783
  567443 -> 566798: 距离 1778.055, 路径 567443 -> 566750 -> 567439 -> 566798
  567443 -> 566802: 距离 963.852, 路径 567443 -> 566742 -> 566802
  567443 -> 566967: 距离 1562.252, 路径 567443 -> 566783 -> 566993 -> 566967
  567443 -> 566993: 距离 988.629, 路径 567443 -> 566783 -> 566993
  567443 -> 566999: 距离 2072.921, 路径 567443 -> 566783 -> 566993 -> 566967 -> 566999   
  567443 -> 567203: 距离 1592.307, 路径 567443 -> 566783 -> 566993 -> 567203
  567443 -> 567238: 距离 780.892, 路径 567443 -> 566783 -> 567238
  567443 -> 567260: 距离 244.053, 路径 567443 -> 567260
  567443 -> 567322: 距离 1582.908, 路径 567443 -> 566742 -> 566802 -> 567322
  567443 -> 567439: 距离 1309.046, 路径 567443 -> 566750 -> 567439
  567443 -> 567443: 距离 0.000, 路径 567443
  567443 -> 567547: 距离 1733.000, 路径 567443 -> 566750 -> 567439 -> 567547
  567443 -> 568098: 距离 810.555, 路径 567443 -> 566742 -> 568098
单独查询目标 33109: 路径 567443 -> 566750 -> 567439 -> 33109 (长度 1956.926)
```

* **结果分析**：
  * 源点 `567443` 到目标点 `33109` 的最短距离为 **1956.926**。
  * 最短路径为：`567443 -> 566750 -> 567439 -> 33109`。

### 4.2 场景二：42个基站顶点 (Sheet 1)

* **运行命令**：
  ```powershell
  python shortest_paths_naive.py --sheet-index 1 --source 565845 --target 565667
  ```
* **运行结果**：

```text
工作表: None | 节点数: 42 | 源点: 565845
源点到各点的距离及路径:
  565845 -> 565675: 距离 1369.368, 路径 565845 -> 567526 -> 567500 -> 565675
  565845 -> 565621: 距离 1928.895, 路径 565845 -> 566010 -> 565631 -> 565801 -> 565630 -> 565621
  565845 -> 565667: 距离 2900.123, 路径 565845 -> 567526 -> 567500 -> 565675 -> 565551 -> 565633 -> 565667
  565845 -> 567510: 距离 645.041, 路径 565845 -> 567526 -> 567510
  565845 -> 565801: 距离 1153.114, 路径 565845 -> 566010 -> 565631 -> 565801
  565845 -> 566010: 距离 403.433, 路径 565845 -> 566010
  565845 -> 567891: 距离 2401.899, 路径 565845 -> 567526 -> 567500 -> 565675 -> 565551 -> 567891
  565845 -> 565492: 距离 2223.014, 路径 565845 -> 567526 -> 567500 -> 565675 -> 565551 -> 565492
  565845 -> 565558: 距离 2171.292, 路径 565845 -> 567526 -> 567500 -> 565675 -> 565551 -> 565558
  565845 -> 565627: 距离 2697.463, 路径 565845 -> 567526 -> 567500 -> 565675 -> 565551 -> 565558 -> 565627
  565845 -> 565572: 距离 2440.918, 路径 565845 -> 567526 -> 567500 -> 566074 -> 565610 -> 565572
  565845 -> 565610: 距离 2025.887, 路径 565845 -> 567526 -> 567500 -> 566074 -> 565610
  565845 -> 565859: 距离 2050.978, 路径 565845 -> 567526 -> 565964 -> 567531 -> 565859
  565845 -> 565630: 距离 1468.955, 路径 565845 -> 566010 -> 565631 -> 565801 -> 565630
  565845 -> 565559: 距离 2381.337, 路径 565845 -> 567526 -> 567500 -> 565675 -> 565516 -> 565559
  565845 -> 565845: 距离 0.000, 路径 565845
  565845 -> 565527: 距离 2594.338, 路径 565845 -> 566010 -> 565631 -> 565801 -> 565630 -> 565648 -> 565527
  565845 -> 565633: 距离 2347.844, 路径 565845 -> 567526 -> 567500 -> 565675 -> 565551 -> 565633
  565845 -> 565496: 距离 2308.237, 路径 565845 -> 566010 -> 565631 -> 565801 -> 565630 -> 565648 -> 565496
  565845 -> 565865: 距离 2489.072, 路径 565845 -> 567526 -> 565964 -> 567531 -> 565859 -> 565865
  565845 -> 565773: 距离 2281.464, 路径 565845 -> 566010 -> 565631 -> 565801 -> 565630 -> 565621 -> 565773
  565845 -> 567531: 距离 1402.786, 路径 565845 -> 567526 -> 565964 -> 567531
  565845 -> 565516: 距离 1918.104, 路径 565845 -> 567526 -> 567500 -> 565675 -> 565516   
  565845 -> 565393: 距离 2339.027, 路径 565845 -> 567526 -> 565964 -> 567531 -> 565859 -> 565393
  565845 -> 565753: 距离 1122.454, 路径 565845 -> 566010 -> 565562 -> 565753
  565845 -> 33566: 距离 2169.678, 路径 565845 -> 566010 -> 565562 -> 565753 -> 567618 -> 33566
  565845 -> 566074: 距离 1573.635, 路径 565845 -> 567526 -> 567500 -> 566074
  565845 -> 565648: 距离 1997.174, 路径 565845 -> 566010 -> 565631 -> 565801 -> 565630 -> 565648
  565845 -> 567526: 距离 488.237, 路径 565845 -> 567526
  565845 -> 565551: 距离 1806.748, 路径 565845 -> 567526 -> 567500 -> 565675 -> 565551   
  565845 -> 565631: 距离 843.923, 路径 565845 -> 566010 -> 565631
  565845 -> 565608: 距离 1883.380, 路径 565845 -> 566010 -> 565562 -> 565753 -> 567618 -> 565608
  565845 -> 567500: 距离 1055.673, 路径 565845 -> 567526 -> 567500
  565845 -> 565531: 距离 2161.483, 路径 565845 -> 566010 -> 565562 -> 565753 -> 567618 -> 565531
  565845 -> 565562: 距离 853.566, 路径 565845 -> 566010 -> 565562
  565845 -> 32788: 距离 2187.665, 路径 565845 -> 567526 -> 565964 -> 567531 -> 565859 -> 32788
  565845 -> 567497: 距离 1561.461, 路径 565845 -> 566010 -> 565562 -> 565753 -> 567497   
  565845 -> 566316: 距离 2592.690, 路径 565845 -> 567526 -> 567500 -> 565675 -> 565551 -> 565558 -> 566316
  565845 -> 568056: 距离 2787.200, 路径 565845 -> 567526 -> 567500 -> 565675 -> 565551 -> 565633 -> 568056
  565845 -> 565964: 距离 741.608, 路径 565845 -> 567526 -> 565964
  565845 -> 567618: 距离 1655.160, 路径 565845 -> 566010 -> 565562 -> 565753 -> 567618   
  565845 -> 565898: 距离 978.426, 路径 565845 -> 566010 -> 565898
单独查询目标 565667: 路径 565845 -> 567526 -> 567500 -> 565675 -> 565551 -> 565633 -> 565667 (长度 2900.123)
```

* **结果分析**：
  * 源点 `565845` 到目标点 `565667` 的最短距离为 **2900.123**。
  * 最短路径为：`565845 -> 567526 -> 567500 -> 565675 -> 565551 -> 565633 -> 565667`。

## 五、 算法性能改进与优化

### 5.1 改进思路

朴素版 Dijkstra 算法的主要性能瓶颈在于**贪心选择**步骤。在每一轮循环中，算法都需要遍历所有未访问节点来寻找距离最小的节点，这导致了 $O(V^2)$ 的时间复杂度。

为了改进性能，我们可以使用**最小优先队列** 来维护未访问节点的距离。

* **优势**：优先队列可以在 $O(\log V)$ 时间内提取最小值，在 $O(\log V)$ 时间内更新节点距离（插入/减小键值）。
* **效果**：这将把寻找最小距离点的总时间从 $O(V^2)$ 降低到 $O(V \log V)$，将松弛更新的总时间从 $O(E)$ 变为 $O(E \log V)$。

### 5.2 优化后的代码实现

以下是使用 Python 标准库 `heapq` 实现的堆优化版 Dijkstra 算法（基于 `shortest_paths.py`）：

```python
import argparse
import heapq
from typing import Dict, List, Tuple

import xlrd


def get_sheet(wb, sheet_name: str | None, sheet_index: int | None):
    if sheet_index is not None:
        return wb.sheet_by_index(sheet_index)
    if sheet_name is None:
        return wb.sheet_by_index(0)
    try:
        return wb.sheet_by_name(sheet_name)
    except xlrd.biffh.XLRDError:
        # 允许将数字字符串视为索引
        if sheet_name.isdigit():
            return wb.sheet_by_index(int(sheet_name))
        raise


def load_graph(path: str, sheet_name: str | None, sheet_index: int | None) -> Tuple[List[int], Dict[int, Dict[int, float]]]:
    """
    解析格式类似于提供的邻接矩阵的工作表。
    第一行包含数字索引头，第二行（索引 1）从第 2 列开始包含节点 ID，
    第 2..n+1 行包含权重，-1 表示没有边。
    """
    wb = xlrd.open_workbook(path)
    sh = get_sheet(wb, sheet_name, sheet_index)

    node_ids: List[int] = []
    # 节点 ID 位于第 1 行（索引 1），从第 2 列（索引 2）开始
    for col in range(2, sh.ncols):
        cell = sh.cell_value(1, col)
        if cell == "" or cell is None:
            continue
        node_ids.append(int(cell))

    graph: Dict[int, Dict[int, float]] = {nid: {} for nid in node_ids}
    for row_idx, nid in enumerate(node_ids, start=2):
        row_node = int(sh.cell_value(row_idx, 1))
        if row_node not in graph:
            continue
        for col_offset, dest in enumerate(node_ids):
            weight = sh.cell_value(row_idx, col_offset + 2)
            if weight == "" or weight is None:
                continue
            if isinstance(weight, (int, float)):
                w = float(weight)
                if w >= 0:
                    graph[row_node][dest] = w
    return node_ids, graph


def dijkstra(graph: Dict[int, Dict[int, float]], source: int):
    dist: Dict[int, float] = {node: float("inf") for node in graph}
    prev: Dict[int, int | None] = {node: None for node in graph}
    if source not in graph:
        raise ValueError(f"源点 {source} 不在图中")

    dist[source] = 0.0
    heap: List[Tuple[float, int]] = [(0.0, source)]

    while heap:
        d, u = heapq.heappop(heap)
        if d > dist[u]:
            continue
        for v, w in graph[u].items():
            nd = d + w
            if nd < dist[v]:
                dist[v] = nd
                prev[v] = u
                heapq.heappush(heap, (nd, v))
    return dist, prev


def reconstruct_path(prev: Dict[int, int | None], target: int) -> List[int]:
    if target not in prev:
        return []
    path: List[int] = []
    cur: int | None = target
    while cur is not None:
        path.append(cur)
        cur = prev[cur]
    path.reverse()
    return path


def run_once(path: str, sheet: str | None, sheet_index: int | None, source: int, target: int | None):
    node_ids, graph = load_graph(path, sheet_name=sheet, sheet_index=sheet_index)
    dist, prev = dijkstra(graph, source)

    print(f"工作表: {sheet} | 节点数: {len(node_ids)} | 源点: {source}")
    print("源点到各点的距离及路径:")
    for nid in node_ids:
        d = dist.get(nid, float("inf"))
        if d == float("inf"):
            print(f"  {source} -> {nid}: 不可达")
        else:
            path_nodes = reconstruct_path(prev, nid)
            path_str = " -> ".join(map(str, path_nodes))
            print(f"  {source} -> {nid}: 距离 {d:.3f}, 路径 {path_str}")

    if target is not None:
        if dist.get(target, float("inf")) == float("inf"):
            print(f"目标 {target} 从 {source} 不可达")
        else:
            shortest_path = reconstruct_path(prev, target)
            print(f"单独查询目标 {target}: 路径 {' -> '.join(map(str, shortest_path))} (长度 {dist[target]:.3f})")


def main():
    parser = argparse.ArgumentParser(description="LTE 基站图上的单源最短路径。")
    parser.add_argument("--file", default="附件1-1.基站图的邻接矩阵-v1-23.xls", help="Excel 邻接矩阵文件")
    parser.add_argument("--sheet", help="要读取的工作表名称 (例如: 方象1 或 方象2)")
    parser.add_argument("--sheet-index", type=int, help="可选的工作表索引 (从 0 开始)，如果名称不方便使用")
    parser.add_argument("--source", type=int, required=True, help="源顶点 ID")
    parser.add_argument("--target", type=int, help="可选的目标顶点 ID，用于打印路径")
    args = parser.parse_args()

    run_once(args.file, args.sheet, args.sheet_index, args.source, args.target)


if __name__ == "__main__":
    main()

```

### 5.3 优化后的复杂度分析

假设图中有 $V$ 个顶点和 $E$ 条边。堆优化版 Dijkstra 算法使用二叉堆来维护未访问节点的距离。

* **时间复杂度**：
  算法主要包含**堆操作（提取最小、插入/更新）**。

  * **提取最小值**：每个节点出队一次，共 $V$ 次。每次操作耗时 $O(\log V)$。总计 $O(V \log V)$。
  * **松弛更新**：每条边可能触发一次距离更新和入堆操作。共 $E$ 次。每次操作耗时 $O(\log V)$。总计 $O(E \log V)$。

  基于此，我们进行分类讨论：

  * **最好情况**：

    * **场景**：图极其稀疏，例如 $E \approx V$（如链状结构）。
    * **分析**：此时 $E$ 与 $V$ 同阶。总时间复杂度为 $O(V \log V + V \log V) = O(V \log V)$。
    * **复杂度**：**$O(V \log V)$**。这是该算法性能最优的场景。
  * **最坏情况**：

    * **场景**：图是稠密图，即 $E \approx V^2$。
    * **分析**：此时 $E$ 占主导地位。总时间复杂度为 $O(V^2 \log V)$。
    * **复杂度**：**$O(V^2 \log V)$**。
    * *注：在此情况下，堆优化的性能（$O(V^2 \log V)$）反而不如朴素实现的 $O(V^2)$，因为维护堆的开销超过了线性扫描的开销。*
  * **平均情况**：

    * **场景**：一般的稀疏图（如本实验中的基站网络，通常每个基站只连接周围少数几个基站）。
    * **分析**：此时 $E \ll V^2$。算法能充分利用堆的特性快速筛选最短路径。
    * **复杂度**：**$O((V + E) \log V)$**。
* **空间复杂度**：

  * **堆空间**：在 Python 的 `heapq` 实现中（懒惰删除模式），堆中可能包含重复的节点记录（旧的距离未被立即删除）。最坏情况下，堆的大小可能达到 $O(E)$。
  * **图与数组**：存储图结构和距离数组需要 $O(V + E)$。
  * **总结**：总空间复杂度为 **$O(V + E)$**。
