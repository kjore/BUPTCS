

## 1. 实验环境与数据

- 环境：Windows，本地编译 `g++ -std=c++11`。
- 源码：`maxCommonStr.cpp`、`maxDescStr.cpp`、`maxSubnums.cpp`。
- 输入：
  - `input.txt（原附件1）`：标签 A/B/C/D 各占一行，其下一行是对应字符串。
  - `input1.txt`、`input2.txt（原附件2）`：整数序列，供 LDS 和最大子段和使用；当前实现对文件内所有数字（含尾部特殊数值）全部参与计算。

## 2. 算法设计与分析

### 2.1 最长公共子序列（LCS）

- 状态：`dp[i][j]` 表示 `s[0..i-1]` 与 `t[0..j-1]` 的 LCS 长度。
- $$
  dp[i][j] = \begin{cases}
  0 & \text{if } i=0 \text{ or } j=0 \\
  dp[i-1][j-1] + 1 & \text{if } X[i] = Y[j] \\
  \max(dp[i-1][j], dp[i][j-1]) & \text{if } X[i] \neq Y[j]
  \end{cases}
  $$
- 回溯：自 `(n,m)` 向前移动，字符相等则收集，否则走向较大值，末尾反转得到一组 LCS。
- 复杂度：时间 `O(n*m)`，空间 `O(n*m)`。

### 2.2 最长递减子序列

- 思路：利用最长公共子序列（LCS）求解。将原序列 `nums` 复制并按降序排序得到 `sortedNums`，则 `nums` 与 `sortedNums` 的 LCS 即为原序列的最长递减子序列。
- 状态：同 LCS 问题，`dp[i][j]` 表示 `nums[0..i-1]` 与 `sortedNums[0..j-1]` 的 LCS 长度。
- 转移：
  $$
  dp[i][j] = \begin{cases}
  dp[i-1][j-1] + 1 & \text{if } nums[i-1] = sortedNums[j-1] \\
  \max(dp[i-1][j], dp[i][j-1]) & \text{if } nums[i-1] \neq sortedNums[j-1]
  \end{cases}
  $$
- 复杂度：时间 `O(n^2)`，空间 `O(n^2)`。
- 输出：通过回溯 `dp` 表构建子序列。

### 2.3 最大子段和

- 思路：线性扫描比较“续接”与“重启”。`dpSum` 为以当前位置结尾的最好和，`maxSum` 记录全局最优及左右端点。
- 状态转移方程：

  $$
  dp[i] = \max(dp[i-1] + a_i, a_i)
  $$

  - 若 `nums[i] + dpSum > nums[i]`，选择续接；否则从 `i` 重启并重置起点。
  - 若新的 `dpSum` 超过历史最佳，则更新 `[left, right]` 与 `maxSum`。
- 复杂度：时间 `O(n)`，空间 `O(1)`。
- 边界：全负数组通过“以首元素初始化”保证返回最大（最不负）值。

## 3. 源代码

### 3.1 `maxCommonStr.cpp`

```cpp
#include<iostream>
#include<string>
#include<algorithm>
#include<fstream>
#include<vector>
using namespace std;

string getLCS(string text1, string text2) {
    int len1 = text1.length();
    int len2 = text2.length();
    vector<vector<int>> dp(len1 + 1, vector<int>(len2 + 1, 0));
  
    for(int i=1;i<=len1;i++){
        for(int j=1;j<=len2;j++){
            if(text1[i-1]==text2[j-1]){
                dp[i][j] = dp[i-1][j-1]+1;
            }
            else{
                dp[i][j] = max(dp[i-1][j],dp[i][j-1]);
            }
        }
    }
  
    // Backtrack to find one LCS string
    string lcs = "";
    int i = len1, j = len2;
    while(i > 0 && j > 0) {
        if (text1[i-1] == text2[j-1]) {
            lcs += text1[i-1];
            i--;
            j--;
        } else if (dp[i-1][j] > dp[i][j-1]) {
            i--;
        } else {
            j--;
        }
    }
    reverse(lcs.begin(), lcs.end());
    return lcs;
}

int main(){
    string A,B,C,D;
    ifstream infile("input.txt");
    if (!infile.is_open()) {
        cerr << "Error: Could not open input.txt" << endl;
        return 1;
    }
    string tmp;
    while(getline(infile,tmp)){
      if(!tmp.empty() && tmp.back() == '\r') tmp.pop_back();

      // Find label prefixes, tolerate "B: " with a trailing space
      if(tmp.find("A:") == 0){
        getline(infile,A);
      }
      else if(tmp.find("B:") == 0){
        getline(infile,B);
      }
      else if(tmp.find("C:") == 0){
        getline(infile,C);
      }
      else if(tmp.find("D:") == 0){
        getline(infile,D);
      }
    }
    // Strip possible trailing CR
    if(!A.empty() && A.back() == '\r') A.pop_back();
    if(!B.empty() && B.back() == '\r') B.pop_back();
    if(!C.empty() && C.back() == '\r') C.pop_back();
    if(!D.empty() && D.back() == '\r') D.pop_back();

    cout<<"LCS(A,B) string: "<<getLCS(A,B)<<endl;
  
    cout<<"LCS(C,D) string: "<<getLCS(C,D)<<endl;

    cout<<"LCS(A,D) string: "<<getLCS(A,D)<<endl;

    cout<<"LCS(C,B) string: "<<getLCS(C,B)<<endl;
   
}
```

### 3.2 `maxDescStr.cpp`

```cpp
#include<iostream>
#include<vector>
#include<fstream>
#include<algorithm>
using namespace std;

void maxDesc(vector<int>&nums){
    if(nums.empty()){
        cout << 0 << endl;
        return;
    }
  
    vector<int> sortedNums = nums;
    sort(sortedNums.begin(), sortedNums.end(), greater<int>());
  
    int n = nums.size();
    int m = sortedNums.size();
  
    vector<vector<int>> dp(n + 1, vector<int>(m + 1, 0));
  
    for(int i = 1; i <= n; i++){
        for(int j = 1; j <= m; j++){
            if(nums[i-1] == sortedNums[j-1]){
                dp[i][j] = dp[i-1][j-1] + 1;
            }else{
                dp[i][j] = max(dp[i-1][j], dp[i][j-1]);
            }
        }
    }
  
    int len = dp[n][m];
    vector<int> lds;
    int i = n, j = m;
    while(i > 0 && j > 0){
        if(nums[i-1] == sortedNums[j-1]){
            lds.push_back(nums[i-1]);
            i--;
            j--;
        }else if(dp[i-1][j] > dp[i][j-1]){
            i--;
        }else{
            j--;
        }
    }
    reverse(lds.begin(), lds.end());
  
    cout << "Length: " << len << endl;
    cout << "Sequence:";
    for(int v : lds) cout << " " << v;
    cout << endl;
}


int main(){
    vector<int> nums1;
    vector<int> nums2;
    int num;
    ifstream infile("input1.txt");
    while(infile >> num){
        nums1.push_back(num);
    }
    infile.close();
    ifstream infile2("input2.txt");
    while(infile2>>num){
        nums2.push_back(num);
    }
    maxDesc(nums1);
    maxDesc(nums2);
    return 0;
}
```

### 3.3 `maxSubnums.cpp`

```cpp
#include<iostream>
#include<vector>
#include<fstream>
#include<algorithm>
using namespace std;

void maxSubnums(vector<int>&nums){
    int n = nums.size();
    int start = 0;
    int maxleft = 0;
    int maxright = 0;
    int maxSum = nums[0];
    int dpSum = nums[0];
    for(int i=1;i<n;i++){
     if(nums[i]<nums[i]+dpSum){
        dpSum = nums[i]+dpSum;
     }else{
        start= i;
        dpSum = nums[i];
     }
     if(dpSum>maxSum){
        maxleft = start;
        maxright = i;
        maxSum = dpSum;
     }
    }
    cout << "最大子段和 = " << maxSum << endl;
    cout << "位置区间（0-based） [" << maxleft << ", " << maxright << "]" << endl;
    for(int i=maxleft;i<=maxright;i++){
        cout<<nums[i]<<' ';
    }
    cout<<endl;
  
}



int main(){
    vector<int> nums1;
    vector<int> nums2;
    int num;
    ifstream infile("input1.txt");
    while(infile >> num){
        nums1.push_back(num);
    }
    infile.close();
    ifstream infile2("input2.txt");
    while(infile2>>num){
        nums2.push_back(num);
    }
    maxSubnums(nums1);
    maxSubnums(nums2);
    return 0;
}
```

## 4. 编译与运行结果

### 4.1 编译命令

```bash
g++ -std=c++11 maxCommonStr.cpp -o maxCommonStr
g++ -std=c++11 maxDescStr.cpp   -o maxDescStr
g++ -std=c++11 maxSubnums.cpp   -o maxSubnums
```

### 4.2 LCS（`maxCommonStr.exe`）

- 四组字符串（A-B / C-D / A-D / C-B）LCS 相同，长度 122：
  `an+algorithm+is+any+welldefined+computational+procedure+that+takes+some+values+as+input+and+produces+some+values+as+output`

![1766411038756](image/report/1766411038756.png)

### 4.3 最长递减子序列（`maxDescStr.exe`）

```
input1.txt
Length: 40
Sequence: 99 99 95 93 89 87 87 79 76 72 68 68 68 60 56 51 50 47 36 31 27 27 27 19 3 0 0 0 -4 -10 -14 -15 -17 -22 -27 -57 -100 -211 -301 -305input2.txt
Length: 30
Sequence: 100 49 47 47 39 38 37 34 34 34 33 28 27 24 24 22 10 8 -10 -11 -16 -25 -32 -38 -39 -42 -44 -70 -304 -307
```

![1766410547713](image/report/1766410547713.png)

### 4.4 最大子段和（`maxSubnums.exe`）

- `input1.txt`：最大子段和 **6914**，区间 `[42, 382]`（0-based，长度 341）

  ```
  64 87 99 39 31 9 99 -2 -7 83 -46 8 16 55 -88 31 -96 51 -60 90 -13 80 50 -88 -9 -84 95 68 -23 24 53 -94 91 60 -34 -19 -53 -40 13 -31 -35 70 25 38 65 49 -99 68 -18 17 79 70 11 -93 93 -24 13 74 70 20 -2 66 97 -20 -56 89 5 -86 87 -56 53 60 73 15 -83 -73 -11 59 -85 87 -24 -81 79 70 -12 29 -4 63 -58 -48 94 20 -68 -10 76 97 72 -56 -45 -96 3 53 60 13 97 65 22 78 99 -12 68 -13 24 -73 -89 22 61 -31 73 5 27 81 -85 55 68 -56 43 60 -19 -23 77 -91 -61 -57 22 -39 -64 29 41 -15 -43 -43 -4 -47 49 -21 66 0 56 45 71 -16 -35 68 60 -26 98 -22 -62 56 51 -63 -83 -62 -48 -33 9 11 5 57 93 35 32 -80 -54 -87 -82 -96 39 93 -89 50 29 47 7 -13 80 23 -85 -38 3 25 36 31 92 46 82 -23 -46 91 89 -40 76 -12 53 -88 -74 27 49 14 42 -60 -32 -43 -18 65 -57 27 27 46 68 -29 63 84 -9 40 -42 -4 -32 -35 82 19 35 -15 84 76 -28 -42 -99 39 79 -54 -9 98 -77 95 -82 -60 -86 3 0 -85 70 -80 33 0 57 73 94 -50 -91 -46 0 42 -98 43 68 -18 -4 25 32 65 -29 -62 -76 78 12 -30 -10 61 94 92 -67 20 -51 33 95 -97 -94 -14 68 31 -15 -55 19 23 -44 25 -17 -48 45 72 37 -22 48 -31 -27 82 16 -20 30 -100 -200 50 60 300 -2 8 -230 45 78 -69 54 29 300 -57 63 70 -120 80 -100 40 20 30 2021 -211 -301 304 -305 307 2023
  ```
- `input2.txt`：最大子段和 **2583**，区间 `[74, 209]`（0-based，长度 136）

  ```
  34 1 -5 40 8 2 6 23 30 42 -4 45 -25 -23 -22 34 -13 -11 -12 16 44 -3 -11 -7 -30 34 49 -47 1 -21 -37 14 33 -37 28 -33 15 -36 36 27 -8 -31 24 -16 -7 38 24 34 48 -27 -22 5 33 9 -26 -2 48 -20 22 38 -42 4 5 -49 10 47 -6 27 8 -10 34 -11 -25 -35 -17 38 43 -9 -8 -16 -25 -32 10 -38 -41 -18 -1 37 1 25 -39 -35 10 -23 -23 -20 43 -4 -42 -9 44 26 -16 41 -1 20 -33 50 -100 130 32 -99 2 -44 36 -15 80 8 15 -15 31 10 20 -40 60 -70 20 30 40 -21 211 301 -304 305 -307 2023
  ```

![1766411071668](image/report/1766411071668.png)

## 5. 代码优化

### 5.1 空间复杂度优化

在求解最长公共子序列（LCS）和利用 LCS 求解最长递减子序列（LDS）时，当前使用了二维数组 `dp[n][m]`，空间复杂度为 $O(n \times m)$。

- **滚动数组优化**：如果只需要求长度而不需要重构具体序列，可以将二维数组压缩为一维滚动数组，将空间复杂度降低至 $O(\min(n, m))$。这在内存受限的情况下非常有用。

### 5.2 时间复杂度优化

- **LDS 的 $O(n \log n)$ 解法**：当前 LDS 通过 LCS 实现，时间复杂度为 $O(n^2)$。可以使用“贪心 + 二分查找”的方法（维护一个单调数组），将时间复杂度优化至 $O(n \log n)$。这种方法在处理大规模数据时效率提升显著。

### 5.3 优化代码实现

#### 5.3.1 LCS 空间优化（滚动数组）

```cpp
int getLCSLengthOptimized(string text1, string text2) {
    int n = text1.length(), m = text2.length();
    // 使用一维滚动数组，空间复杂度降为 O(m)
    vector<int> dp(m + 1, 0);
  
    for (int i = 1; i <= n; i++) {
        int prev = 0; // 记录 dp[i-1][j-1] 的值
        for (int j = 1; j <= m; j++) {
            int temp = dp[j]; // 暂存 dp[i-1][j]
            if (text1[i - 1] == text2[j - 1]) {
                dp[j] = prev + 1;
            } else {
                dp[j] = max(dp[j], dp[j - 1]);
            }
            prev = temp; // 更新 prev 为下一轮的 dp[i-1][j-1]
        }
    }
    return dp[m];
}
```

#### 5.3.2 LDS 时间优化（贪心 + 二分查找）

针对严格递减子序列的 $O(n \log n)$ 解法：

```cpp
#include <vector>
#include <algorithm>
#include <functional> // for greater

int maxDescOptimized(const vector<int>& nums) {
    if (nums.empty()) return 0;
    vector<int> tails;
  
    for (int num : nums) {
        // 如果 tails 为空或 num 小于 tails 的最后一个元素，直接追加
        // 构成更长的递减子序列
        if (tails.empty() || num < tails.back()) {
            tails.push_back(num);
        } else {
            // 否则，在 tails 中找到第一个小于等于 num 的元素并替换它
            // 保持 tails 中的元素尽可能大，以便后续更容易构成递减序列
            // lower_bound 使用 greater<int> 在降序数组中查找第一个 <= num 的位置
            auto it = lower_bound(tails.begin(), tails.end(), num, greater<int>());
            *it = num;
        }
    }
    return tails.size();
}
```

## 6. 结论与改进建议

本次实验成功实现了最长公共子序列（LCS）、最长递减子序列（LDS）和最大子段和三个经典算法问题。

1. **LCS**：通过动态规划求解了四个字符串对的最长公共子序列，验证了算法的正确性。
2. **LDS**：利用 LCS 的思想（原序列与排序序列求 LCS）求解了最长递减子序列，并处理了严格递减的要求，结果与预期一致。
3. **最大子段和**：实现了线性时间复杂度的扫描算法，能够正确处理包含负数的数组，找出了和最大的连续子数组及其区间。

通过本次实验，深入理解了动态规划算法的设计思想，特别是状态定义和状态转移方程的推导。同时，也认识到基础动态规划算法在空间和时间上仍有优化空间，如使用滚动数组降低空间复杂度，或结合二分查找降低时间复杂度。在实际工程应用中，应根据数据规模和资源限制选择最合适的算法实现。
